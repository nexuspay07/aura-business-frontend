import { useId, useState } from "react";

export default function PasswordInput({ label, error, id, className = "", ...props }) { const generatedId = useId(); const inputId = id || generatedId; const [showPassword, setShowPassword] = useState(false); return <div className={`mt-5 ${className}`}><label htmlFor={inputId} className="mb-2 block text-sm font-semibold text-slate-200">{label}</label><div className="relative"><input id={inputId} type={showPassword ? "text" : "password"} className={`h-12 w-full rounded-xl border bg-[#090c12] py-0 pl-4 pr-20 text-[15px] text-white transition hover:border-white/30 focus:border-blue-400 focus:outline-none focus:ring-4 focus:ring-blue-500/20 ${error ? "border-red-400" : "border-white/15"}`} aria-invalid={Boolean(error)} aria-describedby={error ? `${inputId}-error` : undefined} {...props}/><button type="button" aria-label={showPassword ? "Hide password" : "Show password"} className="absolute right-2 top-2 rounded-lg px-3 py-1 text-xs font-semibold text-blue-200 hover:bg-blue-400/10" onClick={() => setShowPassword((current) => !current)}>{showPassword ? "Hide" : "Show"}</button></div>{error && <p id={`${inputId}-error`} className="mt-2 text-sm text-red-200">{error}</p>}</div>; }
