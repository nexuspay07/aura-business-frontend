import { useId } from "react";

export default function AuthInput({ label, error, id, className = "", ...props }) { const generatedId = useId(); const inputId = id || generatedId; return <div className={`mt-5 ${className}`}><label htmlFor={inputId} className="mb-2 block text-sm font-semibold text-slate-200">{label}</label><input id={inputId} className={`h-12 w-full rounded-xl border bg-[#090c12] px-4 text-[15px] text-white placeholder:text-slate-500 transition hover:border-white/30 focus:border-blue-400 focus:outline-none focus:ring-4 focus:ring-blue-500/20 disabled:cursor-not-allowed disabled:opacity-60 ${error ? "border-red-400" : "border-white/15"}`} aria-invalid={Boolean(error)} aria-describedby={error ? `${inputId}-error` : undefined} {...props}/>{error && <p id={`${inputId}-error`} className="mt-2 text-sm text-red-200">{error}</p>}</div>; }
